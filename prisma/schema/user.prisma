model User {
  id            String    @id @default(cuid())
  name          String?
  email         String?   @unique
  image         String?
  password      String?
  emailVerified DateTime?
  accounts      Account[]
  sessions      Session[]

  @@map("users")
}
